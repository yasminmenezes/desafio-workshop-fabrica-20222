// ps.: This is not being used yet.

const Button = () => {
    return (
        <button type="button" className="btn" onClick={getCard}>
            Get another animal
        </button>
    );
};

export default Button;